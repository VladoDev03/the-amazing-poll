class Choice {
    title: string;
    percentage: number;

    constructor(title: string, percentage: number) {
        this.title = title;
        this.percentage = percentage;
    }
}

export default Choice;
